apiVersion: apps/v1
kind: Deployment
metadata:
  name: fares-api
  namespace: default
  labels:
    app: fares-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fares-api
  template:
    metadata:
      labels:
        app: fares-api
    spec:
      containers:
      - name: fares-api
        image: fares-api:latest
        imagePullPolicy: Never  # For minikube local images
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: SERVICE_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_INSTANCES
          value: "http://fares-service:8000"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
