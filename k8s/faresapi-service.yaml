apiVersion: v1
kind: Service
metadata:
  name: fares-api-service
  namespace: default
  labels:
    app: fares-api
spec:
  type: NodePort
  ports:
  - port: 8010
    targetPort: 8000
    protocol: TCP
    nodePort: 30002
    name: http
  selector:
    app: fares-api
